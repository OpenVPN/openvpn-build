This directory or its subdirectories should contain @PRODUCT_NAME@
configuration files each having an extension of .@CONFIG_EXTENSION@.

When @PRODUCT_NAME@ GUI is started, configuration files in this
directory and its subdirectories are added to the list of available
connections.
